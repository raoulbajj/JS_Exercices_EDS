window.onload = function () {
    // EXEMPLE DE L'UTILISATION DE PROMPT :

    // Ici, on déclare une variable name qui va contenir la valeur saisie par l'utilisateur et qui pose la question "What is your name ?"
    let name = prompt("What is your name ? :");

    // Ici, on affiche dans la console le message "Hello" suivi de la valeur de la variable name
    console.log("Hello " + name);
};